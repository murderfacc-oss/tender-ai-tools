# Система: AI-помощник по госзакупкам на монтаж слаботочки

## Цель

Сделать так, чтобы анализ закупки и работа с ней (запросы разъяснений, письма заказчику, жалобы в ФАС, закрывающие документы при исполнении) велись с AI-помощником в едином workspace на каждую закупку.

**Главная боль, которую решаем:** при ручном просмотре что-то упускается, не всегда видны ходы заказчика, не хватает знаний по нормам. Нужен второй мозг, который замечает то, что человек пропускает.

## Подход: workspace per tender

Одна папка = одна закупка = весь её жизненный цикл от первого взгляда до закрывающих документов.

Скиллы — не «генераторы отчётов», а инструменты, которые работают внутри этой папки, читают накопленные артефакты и помогают со следующим шагом (написать запрос разъяснений, подготовить жалобу, сформулировать акт КС-2 и т.д.).

См. структуру в [tenders/README.md](tenders/README.md).

## Текущая фаза: калибровка на 10 реальных закупках

Загружаем 10 реальных закупок в `tenders/`. Смотрим вместе с Claude, чтобы понять:

- что мы реально проверяем глазами (это станет чеклистом)
- где прячутся ходы заказчика и где у нас слепые зоны
- какие документы реально пишутся в работе (запросы, письма, жалобы, закрывающие)
- что общего у всех закупок (каркас) и что уникально (аномалии)

После калибровки соберём финальную архитектуру скиллов исходя из реальных потребностей, а не из гипотез.

## Скиллы

| Скилл | Статус |
|---|---|
| **scan-zakupki** | **v0.2 — установлен в Cowork, в боевой обкатке.** Помощник по проекту закупки: компактный стартовый отчёт по 8 пунктам + свободный диалог + проактивные подсказки на этапах исполнения и сдачи. Работает на простой закупке (тест на закупке Арбитражного суда Северо-Кавказского округа прошёл хорошо). |
| verdikt-zakupki | v1.0 — старый, выключен в Cowork. Лежит как архив. |

## Дальнейшие планы

**На паузе до появления реального тендера.** Пользователь решил: пока тестируем `scan-zakupki` v0.2 на реальных закупках, и только когда появится конкретный тендер для участия — продолжаем разработку.

**Следующий скилл по плану:** `zayavka` — помощник по подготовке заявки на участие. Делаем когда будет конкретный кейс.

**В очереди дальше** (тоже под реальные кейсы, не в вакууме):
- `zakrytie` — закрывающие документы (КС-2, КС-3, исп. документация); схемы — текстовыми подсказками + руки в Visio
- `zhaloba-fas` — жалоба в УФАС (готовый макет от Ипатово как референс)
- `razyasnenie` — возможно не отдельный скилл, а диалог в `scan-zakupki`

## Что забыли (по решению пользователя)

- Семейство «5 фиксированных отчётов» — снято в начале работы.
- Сложная многослойная архитектура с интеграцией ЕИС / Минпромторгом / реестрами — не в этом цикле.
- Калибровочные tenders/ — лежат как история, в активной работе не нужны.
- MCP-направление — будем дорабатывать отдельно, не в одном цикле со скиллами.

## Принципы

- **AI оценивает, но честно метит свои оценки** («это моё прочтение», «требует проверки», «по типовой практике это означает…») и не принимает решение за человека.
- **Главная ценность — полнота охвата.** Ловить то, что человек пропускает.
- **Калибруемся на реальных закупках**, не строим в вакууме.
- **Augmentation, не Automation.** AI готовит — человек решает.
- **Каждая закупка — workspace.** Артефакты накапливаются, контекст не теряется.

## Технические ограничения

- Поле `description` в frontmatter SKILL.md — не более **1024 символов** (ограничение Cowork).
- Скиллы физически живут в Cowork (`/mnt/skills/user/`). Workflow: редактируем здесь → вручную обновляем в Cowork.
